#include <stdio.h>
#include <string.h>
int main()
{
	char a[100];
	int i,j,count;
	scanf("%[^\n]",a);
	for(i=0;a[i]!='\0';i++)
	{
		if(a[i]!=' ')
		{
			count=0;
			for(j=0;a[j]!='\0';j++)
			{
				if(a[i]==a[j] && i!=j)
				{
					count++;
					break;
				}
			}
			if(count==0)
			{
				printf("%c",a[i]);
				break;
			}
		}
	}
}
