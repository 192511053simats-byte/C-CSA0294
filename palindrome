#include <stdio.h>
int main()
{
	int a[100],i,j,n,flag=1;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	for(i=0;i<n/2;i++)
	{
		if(a[i]!=a[n-1-i])
		{
			flag=0;
			break;
		}
	}
	if(flag==1)
	{
		printf("Palindrome");
	}
	else
	{
		printf("Not palindrome");
	}

	
}
